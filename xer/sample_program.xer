char a;
is_capitalized : (char c) -> bool { # 1234567890 123456789012345678901234567890
    return c == 'A' || c == 'B' || c == 'C';
}

int x;

int AB = -1;

guhh : () -> bool {}

incr : () -> void {
    x = x + 1;
    return;
}

decr : (int@ x, int y) -> void {
    @x = @x - y;
    return;
}

struct Data {
    char a;
    char b;
    int c;
};

# just want to annotate some code here...
main : () -> int { # main
    char c = read(); # should read ONE char from stdin

    if (c == 'a') {
        print(c);
        int a = 5;
    }
    elif (is_capitalized(c)) {
        int b = 5;
        print(x);
    }
    else {
        int c = 50;
        for (int i = 0; i < 10; i++) {
            int d = 100;
            print(i);
        }
        while (c != 5) {
            int e = 500;
            print(c);
            if (c) {
                int f = c + e;
                break;
            }
        }
    }

    struct Data G;
    G.a = 'a';
    G.c = 100 * x;
    G.b = 'b';

    struct Data@ arr = new struct Data [5];

    (arr + 0)->a = 'a';
    (arr + 1)->a = 'b';
    (arr + 4)->c = 100 * 5;

    bool BAM = guhh();

    char@ ptr = $c;

    print(arr->a);

    delete (arr); # NOTE: ( ... ) isn't needed for delete
    delete arr;

    return 0;
}